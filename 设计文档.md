# Git Cockpit 最终设计文档

> **版本**：1.1  
> **日期**：2026-08-25  
> **修订记录**：v1.1 确定 "Git Cockpit" 命名体系与 npm 发布标识；确定 SQLite 存储选型与零部署形态  
> **状态**：待开发  
> **目标读者**：开发团队、AI 编程工具、项目维护者

---

## 1. 项目概述

**项目名称**：Git Cockpit（展示名；npm 包：`@shaohui_jin/git-cockpit-mcp-server`，CLI：`git-cockpit`，完整命名规范见 9.1 节）  
**项目定位**：基于 MCP（Model Context Protocol）的 Git 可视化操作工具，提供 Web 端界面与 AI 调用能力，支持多仓库管理、安全的 Git 操作、与 Cursor/VS Code 集成，以及可扩展的 Skill 体系。  
**核心价值**：
- **AI 赋能 Git**：让 AI 助手（Claude、DeepSeek、Cursor 等）通过标准化工具操作 Git 仓库。
- **可视化交互**：提供与 SourceTree 体验一致的 Web 界面，无需安装桌面应用。
- **安全可控**：高风险操作默认禁用，所有写操作有预览、确认、备份和日志审计。
- **多端集成**：Web 独立运行、MCP Server 供 AI 调用、Cursor/VS Code 扩展内嵌 UI。

---

## 2. 目标用户与使用场景

### 2.1 目标用户
- 前端/全栈开发者，希望用 AI 辅助 Git 操作。
- 不熟悉 Git 命令行的开发者，需要可视化界面。
- 团队协作者，需要审查历史、解决冲突。
- 希望将 Git 集成到 AI 工作流的用户。

### 2.2 典型场景
1. **AI 辅助提交**：用户说“看看当前改动并提交”，AI 调用 `git_status`、`git_diff` 查看改动，生成提交信息，调用 `git_add` 和 `git_commit` 完成提交。
2. **历史分析**：AI 调用 `git_log` 获取历史，回答用户关于代码变更的问题。
3. **冲突解决**：AI 调用 `git_status` 发现冲突，`git_diff` 查看冲突内容，给出建议；用户通过 Web 界面手动解决。
4. **可视化手动操作**：用户在 Web 界面点击按钮完成暂存、提交、推送等，操作前有预览确认。
5. **Cursor 内操作**：在 Cursor 编辑器侧边栏打开 Git Cockpit 面板，直接管理当前项目的 Git 状态。
6. **多项目管理**：同时打开多个仓库，快速切换，查看各自状态。

---

## 3. 功能范围

### 3.1 MCP 工具集（供 AI 调用）

#### 只读工具（默认开放）
| 工具名称 | 功能描述 |
|---------|---------|
| `git_status` | 获取工作区状态（已暂存、未暂存、未跟踪文件） |
| `git_log` | 获取提交历史（分页、过滤） |
| `git_diff` | 获取文件差异（支持指定文件、提交范围） |
| `git_show` | 显示提交详细信息 |
| `git_branch_list` | 列出所有本地/远程分支 |
| `git_tag_list` | 列出所有标签 |
| `git_remote_list` | 列出远程仓库信息 |
| `git_file_content` | 获取指定提交中文件的内容 |
| `git_graph` | 获取分支图数据（用于可视化） |

#### 写操作工具（默认开放，需预览/确认）
| 工具名称 | 功能描述 |
|---------|---------|
| `git_add` | 暂存指定文件（支持全部） |
| `git_unstage` | 取消暂存 |
| `git_commit` | 创建提交（带提交信息） |
| `git_checkout` | 切换分支或恢复文件 |
| `git_branch_create` | 创建新分支 |
| `git_branch_delete` | 安全删除分支（不强制） |
| `git_merge` | 合并分支（普通合并） |
| `git_pull` | 拉取远程更改（非强制） |
| `git_push` | 推送到远程（非强制） |
| `git_tag_create` | 创建标签 |
| `git_stash` | 暂存工作区改动 |
| `git_stash_pop` | 恢复 stash |

#### 高风险工具（默认禁用，需用户主动开启或审批）
| 工具名称 | 功能描述 |
|---------|---------|
| `git_reset_hard` | 硬重置到指定提交 |
| `git_clean` | 清理未跟踪文件 |
| `git_push_force` | 强制推送 |
| `git_branch_delete_force` | 强制删除分支 |
| `git_rebase` | 变基操作 |

> **说明**：每个工具均支持 `dry_run` 参数，当为 `true` 时仅返回将执行的命令和影响范围，不真正执行。

### 3.2 Web 可视化界面功能
- **仓库管理**：打开本地仓库、克隆远程仓库、最近打开列表、多仓库标签页切换。
- **状态视图**：展示已暂存、未暂存、未跟踪文件，支持勾选暂存/取消暂存。
- **历史视图**：提交历史列表，支持筛选（作者、日期、消息）、虚拟滚动。
- **分支图**：SVG 或 G6 绘制分支拓扑，可点击节点查看详情。
- **差异查看**：diff2html 渲染并排/行内差异，支持大文件截断提示。
- **操作面板**：提供按钮进行常规操作（暂存、提交、拉取、推送等），操作前弹出预览确认对话框。
- **冲突解决**：可视化冲突标记，提供“保留本地/保留远程/手动编辑”选项。
- **操作日志**：展示所有通过 Web 和 MCP 执行的操作记录（脱敏）。
- **实时同步**：通过 WebSocket/SSE 接收 Git 状态变化，自动刷新界面。
- **权限配置**：图形化界面管理工具白名单/黑名单、域名限制、审批规则。

### 3.3 Cursor/VS Code 扩展功能
- 在侧边栏或面板中嵌入 Web 前端（通过 Webview）。
- 自动检测本地 MCP Server 是否运行，若未运行则自动启动或提示。
- 提供命令：打开 Git Cockpit 面板、刷新状态、快速提交等。
- 支持在编辑器内直接使用 AI 对话调用 Git 工具（如果 Cursor 支持 MCP 客户端）。

### 3.4 Skill 体系（产品层面）
- **Skill 定义**：预定义的 AI 操作流程，如“智能提交”、“冲突分析”、“代码审查”等，封装多个 MCP 工具调用。
- **实现方式**：在 MCP Server 中通过组合工具调用实现，或在 Cursor 扩展中以 Command 形式提供。
- **扩展性**：允许用户自定义 Skill 脚本（JSON 或 JS 配置），并注册到系统中。

---

## 4. 总体架构

### 4.1 系统架构图
```
┌──────────────────────────────────────────────────────────┐
│              AI 客户端 (Claude Desktop, Cursor 等)       │
│                  通过 MCP 协议 (stdio/SSE)              │
└────────────────────────┬─────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────┐
│                   MCP Server (Node.js)                   │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 工具注册中心 (Tool Registry)                       │  │
│  │  - 工具定义、参数 Schema、风险等级                  │  │
│  └────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 权限控制层 (Permission Manager)                    │  │
│  │  - 白名单/黑名单、审批流、操作日志                  │  │
│  └────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────┐  │
│  │ Git 操作服务 (Git Service)                         │  │
│  │  - simple-git 封装、命令构建、结果解析              │  │
│  │  - 串行队列、自动备份、锁管理                       │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────────┬─────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────┐
│               Web 服务器 (Fastify)                       │
│  - REST API 供前端调用                                  │
│  - WebSocket/SSE 实时推送                                │
│  - 认证授权（可选）                                     │
└────────────────────────┬─────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────┐
│          Web 前端 (Vue 3 + Vite + Element Plus)          │
│  - 仓库管理、状态/历史/分支图/差异展示                    │
│  - 操作按钮（预览+确认）                                 │
│  - 实时接收 AI 操作结果                                  │
│  - 权限配置界面                                         │
└──────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────┐
│        Cursor/VS Code 扩展 (TypeScript)                  │
│  - 加载 Web 前端构建产物到 Webview                       │
│  - 与本地 MCP Server 通过 HTTP/WebSocket 通信             │
└──────────────────────────────────────────────────────────┘
```

### 4.2 代码结构（Monorepo）
```
git-cockpit/
├── packages/
│   ├── core/                  # 共享 Git 操作逻辑
│   │   ├── src/
│   │   │   ├── gitService.ts  # simple-git 封装
│   │   │   ├── permissions.ts # 权限管理
│   │   │   ├── backup.ts      # 自动备份
│   │   │   ├── logger.ts      # 日志
│   │   │   └── types.ts
│   │   └── package.json
│   ├── mcp-server/            # MCP 服务器
│   │   ├── src/
│   │   │   ├── index.ts       # 入口
│   │   │   ├── tools/         # 工具定义与实现
│   │   │   └── config.ts
│   │   └── package.json
│   ├── web/                   # Vue 前端
│   │   ├── src/
│   │   │   ├── views/         # 页面组件
│   │   │   ├── components/    # 通用组件
│   │   │   ├── stores/        # Pinia stores
│   │   │   └── api/          # 与后端通信
│   │   └── package.json
│   └── extension/             # Cursor/VS Code 扩展
│       ├── src/
│       │   ├── extension.ts   # 扩展入口
│       │   └── webview.ts     # Webview 管理
│       ├── package.json
│       └── tsconfig.json
├── package.json               # 根 package.json，workspaces
├── pnpm-workspace.yaml
├── tsconfig.base.json
└── README.md
```

---

## 5. 技术选型

| 模块 | 方案 | 说明 |
|------|------|------|
| 后端运行时 | Node.js (≥20) + TypeScript | 生态成熟，MCP SDK 支持好；Node 18 已 EOL，若使用 node:sqlite 则要求 ≥22 |
| MCP SDK | @modelcontextprotocol/sdk | 官方 SDK，支持 stdio、Streamable HTTP 传输（SSE 已被新规范取代） |
| Git 操作 | simple-git | 封装完善，参数数组传递，避免命令注入 |
| Web 框架 | Fastify | 高性能，易于集成 WebSocket |
| 实时通信 | Socket.IO（或 SSE） | 推送状态变化；SSE 更简单，Socket.IO 兼容性好 |
| 前端框架 | Vue 3 (Composition API) + TypeScript | 开发效率高，响应式强 |
| 构建工具 | Vite | 快速开发与构建 |
| 状态管理 | Pinia | Vue 3 官方推荐，轻量 |
| UI 组件库 | Element Plus | 成熟稳定，适合工具类界面 |
| Diff 渲染 | diff2html | 现成 diff 渲染，易于集成 |
| 分支图 | 自研 SVG（推荐）或 @antv/g6 | 自研可定制，G6 功能强大 |
| 数据库 | SQLite（better-sqlite3 或 node:sqlite） | 嵌入式数据库，零部署成本（数据库即本地文件，随 daemon 开关）；操作日志为持续追加型数据，Lowdb 整文件重写模式不适合 |
| 测试 | Vitest + Playwright | 单元测试 + E2E |
| 打包发布 | pnpm + tsup / esbuild | monorepo 管理，快速打包 |
| VS Code 扩展 | TypeScript + VS Code API | 与 Cursor 完全兼容 |
| 部署 | Docker（可选） / pm2 | 便于远程部署 |

---

## 6. 风险把控

### 6.1 风险矩阵

| 风险类别 | 等级 | 影响 | 应对措施 |
|---------|------|------|----------|
| Git 误操作导致数据丢失 | 🔴 高 | 代码永久丢失 | ① 高风险工具默认禁用；② 操作前自动备份（stash/临时分支）；③ 操作预览+二次确认；④ 提供 reflog 恢复界面 |
| 命令注入 | 🔴 高 | 执行恶意命令 | ① 使用 simple-git，参数数组传递；② 校验仓库路径合法性；③ 分支名等严格正则校验 |
| AI 误判执行危险操作 | 🟠 中高 | 错误重置/推送 | ① 高风险工具禁用或审批；② dry-run 模式；③ 工具描述明确风险 |
| 并发冲突 | 🟠 中 | Git 索引损坏 | ① Git Service 串行队列；② 检查 .git/index.lock；③ 所有前端共享同一后端实例 |
| 敏感信息泄露 | 🟠 中高 | 仓库路径、提交内容外泄 | ① 日志脱敏；② 必要认证；③ 最小权限原则 |
| 远程操作误推送 | 🟠 中 | 错误代码推送到远程 | ① push 前展示提交范围；② force push 默认禁用 |
| Web 安全 | 🟡 中 | XSS、CSRF | ① HTTPS；② 输入校验；③ 框架自动转义 |
| 性能问题（大仓库） | 🟡 中 | 卡顿、内存高 | ① 历史列表懒加载+虚拟滚动；② diff 大小限制；③ 后台线程执行 Git 命令 |
| 扩展与 Web 状态不同步 | 🟡 中 | 用户看到过期状态 | ① 所有操作经 MCP Server，前端只通过 WebSocket/SSE 获取更新；② 定期轮询兜底 |

### 6.2 核心安全机制

#### 6.2.1 工具分级与默认权限
- 只读工具：默认开放。
- 写操作工具：默认开放，但需预览确认（可配置为无确认）。
- 高风险工具：默认禁用，需在配置中开启或审批。

#### 6.2.2 操作预览与干运行（dry-run）
- 所有写操作工具支持 `dry_run` 参数，返回将执行的命令和影响范围。
- Web 界面在点击操作按钮后弹出预览对话框，展示命令详情，用户确认后才真正执行。

#### 6.2.3 自动备份与恢复
- 在执行不可逆操作（如 reset --hard、clean、force push）前，自动：
    - 对未提交更改执行 `git stash -u`（带时间戳）。
    - 创建临时分支 `backup/pre-op-{timestamp}` 指向当前 HEAD。
- 提供 reflog 查看和恢复界面，用户可恢复到任意历史状态。

#### 6.2.4 串行队列与锁
- Git Service 内部使用队列确保写操作串行执行。
- 执行前检查 `.git/index.lock`，若存在则等待或报错。
- Web 前端和 MCP 调用共用同一个 Git Service 实例，锁全局生效。

#### 6.2.5 参数校验与安全执行
- 使用 simple-git 的 API，参数以数组传递。
- 对仓库路径进行规范化，确保操作的是用户打开的仓库。
- 分支名、标签名使用正则校验，禁止包含 `;`、`&&`、`|`、空格等特殊字符。

#### 6.2.6 日志与审计
- 记录每次操作的时间、工具名、参数（脱敏）、结果、耗时。
- 日志存储在本地 SQLite 数据库（默认 `~/.git-cockpit/git-cockpit.db`，路径可配置），用户可在界面查看。
- 可配置日志级别和脱敏规则（如隐藏密码、token）。

---

## 7. 进度规划

### 阶段 1：基础架构与只读功能（第 1-2 周）
**目标**：搭建 monorepo，实现 MCP Server 只读工具和 Web 基础界面。

**任务**：
- [ ] 初始化 monorepo（pnpm workspaces），配置 TypeScript、ESLint。
- [ ] 实现 core 模块：Git Service 基础封装（simple-git 初始化、命令执行、结果解析）。
- [ ] 实现 MCP Server 骨架，注册只读工具（status、log、diff、branch_list 等）。
- [ ] 搭建 Fastify Web 服务器，提供基础 REST API（获取仓库状态）。
- [ ] 搭建 Vue 3 前端项目，实现仓库打开、状态列表展示。
- [ ] 集成 WebSocket/SSE，实现状态变化推送。
- [ ] 编写单元测试。

**里程碑**：AI 可通过 MCP 查询仓库信息，Web 界面能实时显示状态。

### 阶段 2：写操作与安全机制（第 3-4 周）
**目标**：实现安全的写操作工具，Web 界面支持手动操作，权限与备份上线。

**任务**：
- [ ] 实现写操作工具（add、commit、checkout、branch 管理等）。
- [ ] 实现 dry-run 和自动备份逻辑。
- [ ] 前端实现操作按钮和确认对话框。
- [ ] 实现串行队列和锁。
- [ ] 实现操作日志记录与查看界面。
- [ ] 集成测试：模拟 AI 调用与 Web 操作。

**里程碑**：用户可通过 Web 或 AI 完成日常 Git 工作流，所有写操作有预览和备份。

### 阶段 3：高级功能与扩展（第 5-6 周）
**目标**：实现合并、冲突解决、rebase、stash 等高级功能，并开发 Cursor 扩展。

**任务**：
- [ ] 实现 git_merge、git_stash、git_tag_create 等工具。
- [ ] 前端实现冲突解决可视化（三方合并视图）。
- [ ] 实现 reflog 查看与恢复界面。
- [ ] 增加权限配置界面（启用/禁用工具、域名限制）。
- [ ] 开发 Cursor/VS Code 扩展壳，加载 Web 构建产物到 Webview。
- [ ] 性能优化：虚拟滚动、diff 懒加载。
- [ ] E2E 测试。

**里程碑**：支持大部分 Git 操作，冲突解决可视化，Cursor 扩展可运行。

### 阶段 4：发布与部署（第 7-8 周）
**目标**：打包发布 npm 包和 Open VSX 扩展，完善文档。

**任务**：
- [ ] 配置 npm 发布流程，发布 `@shaohui_jin/git-cockpit-mcp-server` 包（包含 MCP Server 和 Web 前端）。
- [ ] 配置 Open VSX 发布，打包 Cursor 扩展。
- [ ] 编写用户文档、配置说明。
- [ ] Docker 部署支持。
- [ ] 收集反馈，修复 bug。

**里程碑**：用户可通过 `npx @shaohui_jin/git-cockpit-mcp-server` 启动服务，Cursor 扩展可在市场安装。

---

## 8. 测试策略

### 8.1 单元测试
- core 模块：Git 命令构建、参数校验、解析逻辑。
- 权限控制逻辑、备份机制。
- 工具定义的 Schema 验证。

### 8.2 集成测试
- 使用临时 Git 仓库（`tmp` 目录）模拟各种操作（提交、分支、合并、冲突）。
- 验证 MCP 工具调用与 Web API 调用结果一致性。

### 8.3 E2E 测试（Playwright）
- 模拟 AI 通过 MCP 调用工具，检查 Web 界面同步更新。
- 模拟用户在 Web 界面操作，检查 Git 状态变化。
- 测试危险操作确认流程。
- 测试 Cursor 扩展 Webview 加载和基本操作。

### 8.4 安全测试
- 分支名/路径包含特殊字符（`;`、`&&`、空格等）。
- 并发调用模拟（多客户端同时操作）。
- 权限配置验证（禁用工具返回错误）。

### 8.5 性能测试
- 构造万级提交仓库，测试历史加载速度。
- 大文件（>10MB）diff 处理与截断。

---

## 9. 部署与发布

### 9.1 命名与发布标识

| 标识 | 名称 | 说明 |
|------|------|------|
| 产品展示名 | **Git Cockpit** | Cursor/VS Code 扩展显示名与市场搜索名；与已有扩展 Git Insight 形成 "Insight 负责看、Cockpit 负责开" 的产品家族 |
| npm 主包名 | `@shaohui_jin/git-cockpit-mcp-server` | 唯一的对外发布包，包含 MCP Server、Web 服务与 CLI（发布前需确认 npm 账号名与 scope `@shaohui_jin` 一致） |
| CLI bin 名 | `git-cockpit` | Git 会自动识别 PATH 中 `git-*` 命名的可执行文件，用户可直接使用 `git cockpit` 形式启动 |
| monorepo 内部包 | `@shaohui_jin/git-cockpit-core`、`@shaohui_jin/git-cockpit-web` | 私有包，仅 workspace 内部引用，不单独发布 |
| 扩展 ID | `{publisherId}.git-cockpit` | 发布到 Open VSX；发布前需在市场内复核 "Git Cockpit" 无重名 |
| 本地数据目录 | `~/.git-cockpit/` | 存放 SQLite 数据库文件与配置，路径可配置 |

> 命名依据：已核查 GitHub 与通用搜索，"Git Cockpit" 在 Git 工具领域无冲突；"Git Harbor" 因与 CNCF goharbor 撞名严重而排除。

### 9.2 本地使用
1. 用户通过 npm 安装：`npm install -g @shaohui_jin/git-cockpit-mcp-server`。
2. 运行 `git-cockpit start`（等价于 `git cockpit start`），启动常驻 daemon 进程，内含 MCP Server 和 Web 服务（默认端口 3000）。
3. 浏览器打开 `http://localhost:3000` 操作 Git。
4. 在 AI 客户端（如 Claude Desktop、Cursor）中配置 MCP Server 连接，指向本地 daemon（Streamable HTTP）。
5. SQLite 为嵌入式数据库（单个本地文件，随 daemon 生命周期打开/关闭），用户无需安装或部署任何数据库服务，AI 客户端与扩展也无需感知数据库的存在。

### 9.3 远程部署（可选）
- 使用 Docker 镜像部署到服务器。
- 配置 HTTPS 和用户认证（API Token）。
- 多用户隔离：每个用户只能操作自己有权限的仓库。

### 9.4 Cursor 扩展发布
- 使用 `ovsx` 命令发布到 Open VSX，扩展显示名为 "Git Cockpit"。
- 扩展包中包含 Web 前端构建产物（或从本地服务加载）。
- 扩展启动时自动检测 MCP Server，若未运行则提示用户安装/启动。

### 9.5 配置示例（config.json）
```json
{
  "server": {
    "port": 3000,
    "host": "localhost"
  },
  "storage": {
    "dataDir": "~/.git-cockpit"
  },
  "git": {
    "maxConcurrentOperations": 1,
    "backupOnDangerousOps": true,
    "allowedRepos": ["~/projects/*"]
  },
  "permissions": {
    "disabledTools": ["git_reset_hard", "git_clean", "git_push_force"],
    "requireApprovalFor": ["git_rebase"],
    "dryRunDefault": false
  },
  "logging": {
    "level": "info",
    "redact": ["password", "token"]
  }
}
```

---

## 10. 注意事项与开发关键点

### 10.1 MCP Server 传输方式
- 对于 Cursor 扩展与本地 MCP Server 通信，建议使用 **Streamable HTTP** 而不是 stdio，因为扩展与子进程 stdio 管理复杂（注意：stdio 模式下 MCP Server 是客户端拉起的独立子进程，会破坏 10.3 的"唯一后端"保证，需以 daemon 转发方案规避）。
- 本地 Web 前端通过 HTTP/WebSocket 与 MCP Server 通信。

### 10.2 Webview 安全性
- 在 Cursor/VS Code 扩展的 Webview 中加载本地资源时，需设置 `localResourceRoots` 并启用 `enableScripts`。
- 注意 CSP 设置，避免 XSS 风险。

### 10.3 共享状态同步
- 所有 Git 操作必须通过唯一的后端（MCP Server 中的 Git Service）执行，确保队列和锁全局有效。
- Web 前端与扩展都是“瘦客户端”，只负责展示和触发操作。

### 10.4 打包与版本管理
- npm 包和 VS Code 扩展分别管理版本，建议使用 Changesets 自动化版本发布。
- 使用 GitHub Actions 自动构建、测试、发布。
- 若使用 better-sqlite3（原生 C++ 模块），tsup/esbuild 打包时必须将其配置为 external，不得 bundle 进单文件，依赖在用户安装时走 prebuilt 二进制下载；若采用 Node 22+ 内置的 `node:sqlite` 则无此问题，但需提高 Node 版本门槛。
- Cursor/VS Code 扩展包中不包含任何后端代码与原生模块，仅含 Web 前端构建产物；扩展运行时检测本地 daemon 是否存活，未运行则提示用户安装并启动 `@shaohui_jin/git-cockpit-mcp-server`。

### 10.5 性能优化
- 历史列表使用虚拟滚动（如 `vue-virtual-scroller`）。
- diff 渲染对大文件进行截断或分块加载。
- Git 命令执行放在 Worker 线程或子进程中，避免阻塞事件循环。

### 10.6 用户体验
- 操作按钮在无选中文件时禁用。
- 推送/拉取等远程操作显示进度指示器。
- 错误信息要友好（如“无法推送，远程有更新，请先拉取”）。

---

## 11. 附录

### 11.1 MCP 工具定义示例（TypeScript）
```typescript
import { z } from 'zod';

export const gitStatusTool = {
  name: 'git_status',
  description: '获取当前仓库的工作区状态，包括已暂存、未暂存和未跟踪的文件列表。',
  inputSchema: z.object({
    repoPath: z.string().optional().describe('仓库路径，如果不提供则使用当前打开的仓库')
  }),
  riskLevel: 'readonly'
};
```

### 11.2 操作预览响应格式
```json
{
  "dryRun": true,
  "command": "git add src/index.ts",
  "affectedFiles": ["src/index.ts"],
  "risk": "low",
  "backupCreated": false
}
```

### 11.3 操作日志条目示例
```json
{
  "timestamp": "2025-01-01T12:00:00Z",
  "source": "mcp",
  "tool": "git_commit",
  "params": {
    "message": "fix: resolve login bug"
  },
  "result": "success",
  "durationMs": 120
}
```

---

## 12. 总结

Git Cockpit 是一个结合 AI 与可视化的 Git 管理工具，具备与 SourceTree 相似的操作体验，同时通过 MCP 协议赋予 AI 操作 Git 的能力。项目采用 Vue 3 + Node.js 技术栈，以 monorepo 结构组织，支持 Web、MCP Server 和 Cursor 扩展三种形态。安全机制贯穿始终，确保操作可追溯、可恢复。开发计划分四个阶段，从只读功能逐步扩展到完整工具链，最终发布到 npm 和 Open VSX 市场。

---

**文档结束**  
*后续开发请遵循本设计文档，如有变更需更新版本记录。*