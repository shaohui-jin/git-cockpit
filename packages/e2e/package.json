{
  "name": "@shaohui_jin/git-cockpit-e2e",
  "version": "0.1.19",
  "private": true,
  "type": "module",
  "description": "Git Cockpit Playwright E2E：只测发布形态（build 后的 daemon 静态页），不含产品逻辑",
  "scripts": {
    "test:e2e": "node scripts/run.mjs",
    "install:browsers": "playwright install chromium",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "simple-git": "^3.27.0",
    "typescript": "^5.7.2"
  }
}
