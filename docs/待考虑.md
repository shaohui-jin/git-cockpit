# 待考虑

尚未排期，不是当前开单主流程缺口。Web 用 Token / CLI 开单、设置里导入正文规范，已经可以走通。

## 按仓覆盖模板

现在全文一份 `mr.template`，所有打开的仓共用。若本机同时管两套团队规范（A 仓一套、B 仓另一套），才需要类似 `repoMethods` 的按仓库覆盖。

## 从仓内模板一键导入

仓库里已有 GitLab `.gitlab/merge_request_templates/` 或 GitHub `.github/PULL_REQUEST_TEMPLATE.md` 时，设置页可读仓内文件转成 Cockpit 字段表，少一次手动粘贴。已在设置里导入过 Markdown 则不需要。
